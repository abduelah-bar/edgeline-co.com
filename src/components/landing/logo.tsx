import type { SVGProps } from 'react';

export default function Logo(props: SVGProps<SVGSVGElement>) {
  // Returning null to remove the logo as requested.
  // The text "Placeholder Logo" will be added directly in the hero section.
  return null;
}
